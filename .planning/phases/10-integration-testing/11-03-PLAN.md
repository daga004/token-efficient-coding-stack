---
phase: 10-integration-testing
plan: 03
type: execute
---

<objective>
Verify MCP protocol compliance (v2024-11-05) and error handling for both servers, document protocol gaps, and create Phase 11 synthesis.

Purpose: Ensure both servers follow the MCP specification correctly and handle edge cases gracefully for production use.
Output: Protocol compliance test suite, gap documentation, and Phase 11 integration testing synthesis.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key source files (JSON-RPC handlers):
@auzoom/src/auzoom/mcp/jsonrpc_handler.py
@orchestrator/src/orchestrator/mcp/jsonrpc_handler.py

# Tool schemas:
@auzoom/src/auzoom/mcp/tools_schema.py
@orchestrator/src/orchestrator/mcp/tools_schema.py

# Prior plan summaries:
@.planning/phases/10-integration-testing/11-01-SUMMARY.md
@.planning/phases/10-integration-testing/11-02-SUMMARY.md

**Tech stack available:** pytest, json
**Established patterns:** AuditTest, evidence-based testing
**Constraining decisions:**
- Orchestrator implements initialize handshake (protocol v2024-11-05), AuZoom does NOT
- AuZoom has auzoom_get_calls in handler but NOT in tools_schema.py manifest (discrepancy)
- AuZoom JSON-RPC handler: sync, methods tools/list and tools/call only
- Orchestrator JSON-RPC handler: async tools/call, sync initialize and tools/list
- MCP spec v2024-11-05 requires initialize handshake
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test MCP protocol compliance for both servers</name>
  <files>audit/tests/test_mcp_protocol.py</files>
  <action>Create pytest test file verifying MCP protocol compliance:

1. **JSON-RPC 2.0 response format (both servers):**
   - Call tools/list → verify response has jsonrpc: "2.0", id matches request, result contains tools array
   - Call tools/call with valid tool → verify response has jsonrpc: "2.0", result.content array with text type
   - Verify each response is valid JSON-RPC (no missing fields)

2. **Tool manifest completeness:**
   - AuZoom: Verify manifest lists all tools that handle_tool_call actually supports. NOTE: auzoom_get_calls exists in server.py handler dict but is MISSING from tools_schema.py manifest - document this as a finding
   - Orchestrator: Verify manifest lists all 3 tools (route, execute, validate) matching handler
   - For each tool: verify name, description, inputSchema with required properties

3. **Initialize handshake (MCP v2024-11-05):**
   - Orchestrator: Send initialize request → verify response includes protocolVersion "2024-11-05", capabilities.tools, serverInfo
   - AuZoom: Send initialize request → verify it returns error (method not found) - document as protocol gap
   - This is a known gap: AuZoom lacks initialize handshake

4. **Error codes compliance:**
   - Both servers: Send unknown method → verify -32601 (Method not found) error code
   - Both servers: Send malformed JSON → verify -32700 (Parse error) error code
   - Both servers: Send unknown tool name via tools/call → verify error dict returned
   - Verify error responses include jsonrpc: "2.0", error.code, error.message

Test directly against JSONRPCHandler._handle_request (AuZoom) and JSONRPCHandler._handle_request / _handle_request_async (Orchestrator) to avoid stdin/stdout complexity. Also test the server-level handle_tool_call for error cases.</action>
  <verify>cd /Users/dhirajd/Documents/claude && python -m pytest audit/tests/test_mcp_protocol.py -v 2>&1 | tail -30</verify>
  <done>All protocol compliance tests pass, AuZoom missing initialize handshake documented, auzoom_get_calls manifest gap documented</done>
</task>

<task type="auto">
  <name>Task 2: Error handling edge cases and Phase 11 synthesis</name>
  <files>audit/tests/test_mcp_protocol.py, audit/evidence/11-03-protocol-compliance.jsonl, audit/reports/11-PHASE-SYNTHESIS.md</files>
  <action>Part A - Extend protocol test file with error handling edge cases:

1. **Missing required parameters:**
   - AuZoom auzoom_read without "path" → verify error (not crash)
   - Orchestrator orchestrator_route without "task" → verify error (not crash)
   - Both should return dict with "error" key

2. **Invalid parameter types:**
   - AuZoom auzoom_read with level="invalid_level" → verify graceful handling
   - Orchestrator orchestrator_route with task=123 (not string) → verify handling
   - AuZoom auzoom_read with path to nonexistent file → verify error message

3. **Empty/null arguments:**
   - Both servers: Call with empty arguments {} → verify error or default behavior
   - Both servers: Call with None arguments → verify graceful handling

4. **Evidence collection:**
   - Log all protocol test results to audit/evidence/11-03-protocol-compliance.jsonl
   - Include: test name, server, request, response, compliance status, gaps found

Part B - Create Phase 11 synthesis report at audit/reports/11-PHASE-SYNTHESIS.md:

Structure:
- Phase 11 Overview (Integration Testing)
- Workflow Integration (Plan 11-01 findings)
- Conflict Testing (Plan 11-02 findings)
- Protocol Compliance (Plan 11-03 findings)
- Protocol Gaps Found:
  1. AuZoom missing initialize handshake (MCP v2024-11-05 compliance gap)
  2. auzoom_get_calls tool in handler but not in manifest (tool schema discrepancy)
  3. Any other gaps discovered during testing
- Gap Severity Classification (Critical/Important/Enhancement for each gap)
- V1 Impact Assessment (do gaps block V1 certification?)
- Recommendations for Phase 13 (Critical Fixes)

Include actual test counts, pass rates, and specific findings from all 3 plans.</action>
  <verify>cd /Users/dhirajd/Documents/claude && python -m pytest audit/tests/test_mcp_protocol.py -v 2>&1 | tail -30 && test -f audit/evidence/11-03-protocol-compliance.jsonl && test -f audit/reports/11-PHASE-SYNTHESIS.md && echo "All artifacts exist"</verify>
  <done>All error handling tests pass, evidence collected, Phase 11 synthesis report created with gap classification</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest audit/tests/test_mcp_protocol.py -v` passes all tests
- [ ] Initialize handshake gap documented for AuZoom
- [ ] Tool manifest discrepancy documented (auzoom_get_calls)
- [ ] Error handling verified for both servers (missing params, invalid types, nonexistent files)
- [ ] Evidence file exists at audit/evidence/11-03-protocol-compliance.jsonl
- [ ] Phase 11 synthesis report exists at audit/reports/11-PHASE-SYNTHESIS.md
- [ ] All gaps classified by severity
</verification>

<success_criteria>

- All protocol compliance tests pass
- All error handling tests pass
- Protocol gaps documented with severity classification
- Phase 11 synthesis complete with V1 impact assessment
- Evidence collected for all 3 plans
- Phase 11: Integration Testing COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-testing/11-03-SUMMARY.md`

Include Phase completion note: "Phase 11 complete, ready for Phase 12 (Gap Analysis & Reporting)"
</output>
