---
phase: 10-integration-testing
plan: 02
type: execute
---

<objective>
Test for conflicts between AuZoom and Orchestrator when operating together: caching coherency, model selection consistency, and tool isolation.

Purpose: Ensure both MCP servers can coexist without interference, stale state, or behavioral conflicts.
Output: Conflict test suite with evidence documenting no interference between systems.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key source files:
@auzoom/src/auzoom/mcp/server.py
@auzoom/src/auzoom/core/graph/lazy_graph.py
@orchestrator/src/orchestrator/mcp/server.py
@orchestrator/src/orchestrator/scoring.py

# Prior plan summary:
@.planning/phases/10-integration-testing/11-01-SUMMARY.md

# Related findings:
@.planning/phases/02-auzoom-core-verification/02-03-SUMMARY.md

**Tech stack available:** pytest, tiktoken
**Established patterns:** AuditTest base class, cache testing from Phase 2
**Constraining decisions:**
- Phase 2-03: Bypass behavior tested (75% cache hit rate)
- AuZoom caches in .auzoom/summaries/ and .auzoom/metadata/
- Orchestrator is stateless (no caching layer)
- AuZoom sync, Orchestrator async - potential event loop contention
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test caching coherency and model selection consistency</name>
  <files>audit/tests/test_conflicts.py</files>
  <action>Create pytest test file testing for conflicts between the two systems:

1. **AuZoom cache coherency under workflow:**
   - Read a Python file at skeleton level → get cached result
   - Modify the file content (write a temp change, revert after)
   - Re-read the file → verify cache is invalidated and new content returned
   - This tests that file modifications during a workflow are detected correctly
   - Use a temp copy of a real file to avoid modifying source

2. **Orchestrator routing determinism:**
   - Route the same task description 5 times → verify identical scores and model recommendations each time
   - Route with varying context (files_count: 1 vs 10) → verify score changes appropriately
   - This confirms no hidden state accumulation between routing calls

3. **Cross-server state isolation:**
   - Instantiate AuZoomMCPServer and OrchestratorMCPServer
   - Call auzoom_read multiple times (building cache)
   - Call orchestrator_route between auzoom calls
   - Verify auzoom cache is unaffected by orchestrator calls
   - Verify orchestrator routing is unaffected by auzoom state
   - This tests that the servers don't share mutable state

Use tmp_path pytest fixture for file modifications. Clean up .auzoom cache in fixtures.</action>
  <verify>cd /Users/dhirajd/Documents/claude && python -m pytest audit/tests/test_conflicts.py -v 2>&1 | tail -30</verify>
  <done>All conflict tests pass: cache coherency verified, routing deterministic, no cross-server interference</done>
</task>

<task type="auto">
  <name>Task 2: Test tool dispatch isolation and async/sync compatibility</name>
  <files>audit/tests/test_conflicts.py, audit/evidence/11-02-conflicts.jsonl</files>
  <action>Extend the conflict test file with tool isolation and compatibility tests:

1. **Tool name dispatch isolation:**
   - Call AuZoom server with orchestrator tool name (e.g., "orchestrator_route") → verify clean error (not crash)
   - Call Orchestrator server with auzoom tool name (e.g., "auzoom_read") → verify clean error
   - Verify error format is proper JSON-RPC error dict with "error" key
   - This confirms tool namespacing prevents cross-dispatch

2. **Async/sync compatibility:**
   - Test that AuZoom's synchronous handle_tool_call works correctly when called from async context (using asyncio.run or loop.run_in_executor)
   - Test that Orchestrator's async handle_tool_call works correctly when wrapped with asyncio.run for sync callers
   - Both patterns are how Claude Code would invoke them

3. **Concurrent-like access pattern:**
   - Rapidly alternate between auzoom_read and orchestrator_route calls (10 interleaved calls)
   - Verify all calls return valid results (no corruption, no shared state bleeding)
   - Log timing for each call to evidence

4. **Evidence collection:**
   - Log all conflict test results to audit/evidence/11-02-conflicts.jsonl
   - Record: test name, servers involved, result, timing, pass/fail</action>
  <verify>cd /Users/dhirajd/Documents/claude && python -m pytest audit/tests/test_conflicts.py -v 2>&1 | tail -30 && test -f audit/evidence/11-02-conflicts.jsonl && echo "Evidence exists"</verify>
  <done>All isolation tests pass: tool dispatch correctly errors for unknown tools, async/sync compatibility confirmed, interleaved calls produce correct results</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest audit/tests/test_conflicts.py -v` passes all tests
- [ ] Cache coherency verified (file changes detected after modification)
- [ ] Routing determinism verified (same input → same output)
- [ ] Tool dispatch isolation verified (cross-server calls error cleanly)
- [ ] Async/sync compatibility verified
- [ ] Evidence file exists at audit/evidence/11-02-conflicts.jsonl
</verification>

<success_criteria>

- All conflict tests pass
- No cross-server state interference detected
- Cache invalidation works correctly in workflow context
- Tool namespacing prevents cross-dispatch errors
- Async/sync boundary works correctly
- Evidence collected
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-testing/11-02-SUMMARY.md`
</output>
