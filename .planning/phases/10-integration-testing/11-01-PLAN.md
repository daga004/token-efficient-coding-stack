---
phase: 10-integration-testing
plan: 01
type: execute
---

<objective>
Test end-to-end AuZoom + Orchestrator workflow to verify the full tool chain works correctly when both servers are used together.

Purpose: Validate that the intended usage pattern (route task → read context → execute) produces correct results across complexity tiers.
Output: Integration test suite proving workflow correctness with evidence.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key source files (both MCP servers):
@auzoom/src/auzoom/mcp/server.py
@orchestrator/src/orchestrator/mcp/server.py

# Existing test patterns:
@auzoom/tests/test_mcp_server.py
@audit/tests/test_integration_optimizations.py

# Prior summaries (AuZoom + Orchestrator verification):
@.planning/phases/02-auzoom-core-verification/02-01-SUMMARY.md
@.planning/phases/04-orchestrator-core-verification/04-01-SUMMARY.md

**Tech stack available:** pytest, pydantic, tiktoken
**Established patterns:** AuditTest base class, evidence-based testing, JSON Lines logging
**Constraining decisions:**
- Phase 2: AuZoom tools verified individually (95.32% skeleton reduction)
- Phase 4: Orchestrator routing verified (90% appropriateness, 100% quality)
- Phase 6.5: Progressive disclosure validated (71.3% savings)
- Both servers are independent (no cross-imports) - integration at Claude Code level
- AuZoom is sync, Orchestrator is async
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create end-to-end workflow integration test</name>
  <files>audit/tests/test_e2e_workflow.py</files>
  <action>Create pytest test file that instantiates both AuZoomMCPServer and OrchestratorMCPServer and tests the intended workflow:

1. **Route → Read → Context Assembly pattern:**
   - Call orchestrator_route() with a task description → get model recommendation and complexity score
   - Based on complexity, call auzoom_read() at appropriate level (skeleton for simple, summary for medium, full for complex)
   - Verify the context from auzoom_read is usable as input to orchestrator_execute()

2. **Test 3 complexity scenarios:**
   - Simple task (score 0-3): "List all functions in models.py" → routes to Flash, skeleton read sufficient
   - Medium task (score 3-5): "Explain how auzoom_read handles Python vs non-Python files" → routes to Haiku, summary read needed
   - Complex task (score 5-8): "Refactor the MCP server to separate concerns" → routes to Sonnet, full read needed

3. **Verify workflow correctness for each scenario:**
   - orchestrator_route returns valid model recommendation with score
   - auzoom_read returns content at the requested level (not error)
   - Content format is consistent (dict with expected keys)
   - Token counts from auzoom are reasonable for the level (skeleton < summary < full)

Use AuZoomMCPServer('.', auto_warm=False) for AuZoom. Use OrchestratorMCPServer() for Orchestrator (call with asyncio.run for async methods).

Do NOT test actual model execution (orchestrator_execute calls real APIs). Focus on routing + context assembly correctness.

Follow existing test patterns from auzoom/tests/test_mcp_server.py (pytest fixtures, direct handle_tool_call).</action>
  <verify>cd /Users/dhirajd/Documents/claude && python -m pytest audit/tests/test_e2e_workflow.py -v 2>&1 | tail -30</verify>
  <done>All e2e workflow tests pass: route→read→context for 3 complexity scenarios verified</done>
</task>

<task type="auto">
  <name>Task 2: Test tool chaining data flow and token efficiency</name>
  <files>audit/tests/test_e2e_workflow.py, audit/evidence/11-01-e2e-workflow.jsonl</files>
  <action>Extend the test file with additional tests verifying data flow correctness and token efficiency across the tool chain:

1. **Data format compatibility:**
   - Verify auzoom_read output structure has keys expected by downstream consumers (type, level, nodes/content)
   - Verify orchestrator_route output has model, score, tier, cost_estimate
   - Verify both servers return well-formed dicts (not raw strings)

2. **Progressive disclosure token savings in workflow context:**
   - For each complexity tier, measure tokens from auzoom_read at different levels
   - Verify skeleton tokens < summary tokens < full tokens for Python files
   - Verify the savings align with Phase 6.5 findings (>50% reduction at skeleton vs full)

3. **Non-Python file handling in workflow:**
   - Test auzoom_read on a non-Python file (e.g., README.md or pyproject.toml)
   - Verify metadata summary is returned (not raw content by default)
   - Verify the metadata is useful for context assembly

4. **Evidence collection:**
   - Log results to audit/evidence/11-01-e2e-workflow.jsonl using audit harness patterns
   - Record: task description, route result, read level, token count, pass/fail

Create a simple evidence collection at the end of the test module (write JSON Lines to evidence file).</action>
  <verify>cd /Users/dhirajd/Documents/claude && python -m pytest audit/tests/test_e2e_workflow.py -v 2>&1 | tail -30 && test -f audit/evidence/11-01-e2e-workflow.jsonl && echo "Evidence exists"</verify>
  <done>All data flow tests pass, token efficiency verified (skeleton < summary < full), evidence file created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest audit/tests/test_e2e_workflow.py -v` passes all tests
- [ ] Evidence file exists at audit/evidence/11-01-e2e-workflow.jsonl
- [ ] 3 complexity scenarios tested (simple, medium, complex)
- [ ] Token ordering verified (skeleton < summary < full)
- [ ] Non-Python file handling tested
</verification>

<success_criteria>

- All workflow tests pass
- Route → Read → Context pattern verified for 3 complexity tiers
- Token efficiency confirmed across progressive disclosure levels
- Data format compatibility verified between servers
- Evidence collected
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-testing/11-01-SUMMARY.md`
</output>
