---
phase: 13-critical-fixes-v11-roadmap
plan: 01
type: execute
domain: auzoom-structured-code
---

<objective>
Implement the 3 open Important gaps (GAP-023, GAP-024, GAP-025) that are conditions for V1 certification.

Purpose: Satisfy CONDITIONAL GO requirements — fix MCP protocol compliance and error handling for portability to non-Claude-Code MCP hosts.
Output: 3 fixes implemented (~55 lines across 3 files), all existing tests still pass.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 12 certification report with exact fix specifications:
@audit/reports/12-V1-CERTIFICATION.md

# Source files to modify:
@auzoom/src/auzoom/mcp/jsonrpc_handler.py
@auzoom/src/auzoom/mcp/tools_schema.py
@orchestrator/src/orchestrator/mcp/server.py

# Prior phase summary (dependency graph):
@.planning/phases/11-gap-analysis-reporting/12-02-SUMMARY.md

**Constraining decisions:**
- Phase 12-02: CONDITIONAL GO verdict — these 3 fixes are the conditions
- Phase 11-03: Protocol compliance testing identified these 3 gaps
- Phase 10-03: Portability is primary filter for V1 skill design

**Issues being addressed:**
- GAP-023: AuZoom missing MCP initialize handshake (Important)
- GAP-024: auzoom_get_calls not in tool manifest (Important)
- GAP-025: Orchestrator uncaught Pydantic ValidationError (Important)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MCP initialize handshake to AuZoom (GAP-023)</name>
  <files>auzoom/src/auzoom/mcp/jsonrpc_handler.py</files>
  <action>
  Add initialize method handling to JSONRPCHandler:

  1. In `_handle_request()` method (around line 33), add an elif branch before the else clause:
     `elif method == "initialize": return self._handle_initialize(request)`

  2. Create new `_handle_initialize(self, request: dict) -> dict` method following the pattern of `_handle_tools_list()` (lines 41-48). Return JSON-RPC response with result containing:
     - protocolVersion: "2024-11-05"
     - capabilities: {"tools": {}}
     - serverInfo: {"name": "auzoom", "version": "1.0.0"}

  Follow existing response format: {"jsonrpc": "2.0", "id": request.get("id"), "result": {...}}

  Do NOT add notifications/initialized handling — that is a notification (no response needed per MCP spec).
  </action>
  <verify>python -c "from auzoom.mcp.jsonrpc_handler import JSONRPCHandler; print('Import OK')" works from auzoom/ directory</verify>
  <done>_handle_initialize method exists, returns protocolVersion "2024-11-05" with tools capability and serverInfo</done>
</task>

<task type="auto">
  <name>Task 2: Add auzoom_get_calls to tool manifest (GAP-024)</name>
  <files>auzoom/src/auzoom/mcp/tools_schema.py</files>
  <action>
  Add the missing auzoom_get_calls tool schema:

  1. In `get_tools_manifest()` function (around line 12), add `_auzoom_get_calls_schema()` to the tools list. Currently lists 5 tools — this makes it 6.

  2. Create new `_auzoom_get_calls_schema() -> dict` function following the existing pattern (e.g., `_auzoom_find_schema()` at lines 49-64). Schema:
     - name: "auzoom_get_calls"
     - description: "Get function/method calls made by a specific node in the code graph"
     - inputSchema with required "node_id" (string) parameter described as "Fully qualified node ID (e.g., 'module.py::ClassName.method_name')"

  Place the new function after the last existing schema function (after line 122), following the same pattern.
  </action>
  <verify>python -c "from auzoom.mcp.tools_schema import get_tools_manifest; t = get_tools_manifest(); print(f'{len(t[\"tools\"])} tools'); names = [x['name'] for x in t['tools']]; assert 'auzoom_get_calls' in names, 'Missing!'; print('OK')" from auzoom/ directory</verify>
  <done>get_tools_manifest() returns 6 tools (was 5), auzoom_get_calls is discoverable via tools/list</done>
</task>

<task type="auto">
  <name>Task 3: Catch ValidationError in Orchestrator handlers (GAP-025)</name>
  <files>orchestrator/src/orchestrator/mcp/server.py</files>
  <action>
  Add Pydantic ValidationError handling to 3 methods in OrchestratorMCPServer:

  1. Add import at top: `from pydantic import ValidationError`

  2. In `_route()` method (line ~70): Wrap `task = Task(description=task_description, context=context)` in try/except ValidationError. On catch, return `{"error": f"Invalid task parameters: {e}", "type": "validation_error"}`.

  3. In `_execute()` method: If Task() is called directly (check if it is — it may receive task as dict/string), add similar wrapping. If _execute() doesn't create Task objects, skip this one.

  4. In `_validate()` method: Same pattern — wrap Task() creation if present.

  Follow existing error return pattern in the class (e.g., line 47: `{"error": "Unknown tool: ..."}` dict format). Do NOT raise exceptions — return error dicts for all ValidationError cases.

  Keep changes minimal (~5 lines per handler that creates Task objects).
  </action>
  <verify>python -c "from orchestrator.mcp.server import OrchestratorMCPServer; print('Import OK')" from orchestrator/ directory</verify>
  <done>All handlers that create Task() objects catch ValidationError and return structured error dicts instead of propagating raw exceptions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 3 source files modified with fixes
- [ ] Each modified file imports successfully (no syntax errors)
- [ ] _handle_initialize returns correct MCP response structure
- [ ] get_tools_manifest() returns 6 tools including auzoom_get_calls
- [ ] ValidationError in Orchestrator returns error dict, not raw exception
- [ ] Total changes ~55 lines across 3 files
</verification>

<success_criteria>

- All 3 GAP fixes implemented
- No import or syntax errors introduced
- Changes follow existing code patterns
- Each fix is independently correct
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes-v11-roadmap/13-01-SUMMARY.md`
</output>
