---
phase: 13-critical-fixes-v11-roadmap
plan: 02
type: execute
domain: auzoom-structured-code
---

<objective>
Verify that the 3 critical fixes (GAP-023, GAP-024, GAP-025) work correctly by updating existing gap-documenting tests and running the full Phase 11 regression suite.

Purpose: Satisfy V1 certification condition #4 — "Re-run Phase 11 protocol compliance tests to verify fixes."
Output: All 86+ tests pass (84 existing + updated gap tests), 0 regressions, fixes verified.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan in this phase:
@.planning/phases/12-critical-fixes-v11-roadmap/13-01-SUMMARY.md

# Test files to update:
@audit/tests/test_mcp_protocol.py

# Other test files for regression:
@audit/tests/test_e2e_workflow.py
@audit/tests/test_conflicts.py

# V1 certification (verification requirements):
@audit/reports/12-V1-CERTIFICATION.md

**Constraining decisions:**
- Phase 12-02: "Re-run Phase 11 protocol compliance tests to verify fixes"
- Phase 13-01: Fixes implemented (initialize handshake, tool manifest, ValidationError)

**Test updates needed:**
The following tests currently EXPECT gaps. They must be updated to VERIFY fixes:
1. `test_auzoom_lacks_initialize` (lines 428-457) → test that initialize NOW returns protocolVersion
2. `test_auzoom_get_calls_manifest_gap` (lines 268-298) → test that auzoom_get_calls IS in manifest
3. `test_auzoom_manifest_tool_count` (lines 246-266) → assert 6 tools (was 5)
4. `test_orchestrator_route_task_as_integer` (lines 736-764) → test that error dict returned (not ValidationError)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update gap-documenting tests to verify fixes</name>
  <files>audit/tests/test_mcp_protocol.py</files>
  <action>
  Update 4 existing tests that currently document gaps to instead verify fixes:

  1. **test_auzoom_lacks_initialize** → Rename to `test_auzoom_initialize_handshake`. Change assertions:
     - Remove: assert response has error code -32601
     - Add: assert response has "result" with "protocolVersion" == "2024-11-05"
     - Add: assert "capabilities" has "tools" key
     - Add: assert "serverInfo" has "name" == "auzoom"
     - Update compliance status from "gap" to "pass"
     - Update evidence description

  2. **test_auzoom_get_calls_manifest_gap** → Rename to `test_auzoom_get_calls_in_manifest`. Change assertions:
     - Remove: assert auzoom_get_calls NOT in manifest names
     - Add: assert "auzoom_get_calls" IN manifest tool names
     - Assert schema has inputSchema with required "node_id" property
     - Update compliance status from "gap" to "pass"

  3. **test_auzoom_manifest_tool_count** → Update expected count:
     - Change: assert tool count is 5 → assert tool count is 6
     - Update compliance note from "partial - auzoom_get_calls missing" to "pass - all tools listed"

  4. **test_orchestrator_route_task_as_integer** → Change from expecting exception to expecting error dict:
     - Remove: pytest.raises(ValidationError) or similar exception assertion
     - Add: assert result is a dict with "error" key containing validation message
     - Add: assert "type" key is "validation_error"
     - Update compliance status from "gap" to "pass"

  Keep the evidence collection pattern (writing to JSONL) — just update the expected compliance status from "gap" to "pass".

  Also update the file header comments (around lines 9-11) that document known gaps — remove or update the gap descriptions since they're now fixed.
  </action>
  <verify>python -c "import ast; ast.parse(open('audit/tests/test_mcp_protocol.py').read()); print('Syntax OK')"</verify>
  <done>4 tests updated to verify fixes instead of documenting gaps, all test names reflect positive verification, compliance statuses changed from "gap" to "pass"</done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 11 test suite and verify</name>
  <files>audit/tests/test_mcp_protocol.py, audit/tests/test_e2e_workflow.py, audit/tests/test_conflicts.py</files>
  <action>
  Run the complete Phase 11 test suite to verify fixes and check for regressions:

  1. Run: `cd /Users/dhirajd/Documents/claude && python -m pytest audit/tests/test_mcp_protocol.py audit/tests/test_e2e_workflow.py audit/tests/test_conflicts.py -v --tb=short 2>&1`

  2. Verify:
     - All 86+ tests pass (was 84 existing + updates to 4 gap tests)
     - The 4 updated tests specifically pass:
       a. test_auzoom_initialize_handshake → PASS
       b. test_auzoom_get_calls_in_manifest → PASS
       c. test_auzoom_manifest_tool_count → PASS (6 tools)
       d. test_orchestrator_route_task_as_integer → PASS (error dict)
     - No regressions in any other tests

  3. If any test fails:
     - Investigate root cause (is it the fix or the test update?)
     - Fix the issue (prefer fixing the test if the fix is correct, or fix the fix if the test reveals a real problem)
     - Re-run until all pass

  4. Record test results in evidence format for the summary.
  </action>
  <verify>All tests pass with 0 failures, 0 errors</verify>
  <done>86+ tests pass (0 failures, 0 errors), 4 gap tests now verify fixes, no regressions in existing tests</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 4 gap-documenting tests updated to verify fixes
- [ ] Full test suite passes (86+ tests, 0 failures)
- [ ] AuZoom initialize test confirms protocolVersion "2024-11-05"
- [ ] AuZoom manifest test confirms 6 tools
- [ ] Orchestrator ValidationError test confirms error dict response
- [ ] No regressions in any existing tests
- [ ] Evidence JSONL files updated with new compliance statuses
</verification>

<success_criteria>

- All tests pass with 0 failures
- 4 previously-gap tests now pass as verification tests
- No regressions in 82+ other tests
- V1 certification condition #4 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/12-critical-fixes-v11-roadmap/13-02-SUMMARY.md`
</output>
